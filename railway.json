{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r warmup_requirements.txt"
  },
  "deploy": {
    "cronSchedule": "0 * * * *",
    "startCommand": "python3 warmup_imap.py",
    "restartPolicyType": "NEVER"
  }
}
