{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pip3 install -r warmup_requirements.txt"
  },
  "deploy": {
    "cronSchedule": "0 * * * *",
    "startCommand": "python3 warmup_imap.py",
    "restartPolicyType": "NEVER"
  }
}
